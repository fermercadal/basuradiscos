---
interface Props {
  artFront: string;
  artBack: string;
}

const { artFront, artBack } = Astro.props;
---

<section class="py-6">
  <h3 class="sr-only">Artwork</h3>
  <div class="flex flex-row flex-wrap gap-6">
    <img width={400} height={400} src={artFront} alt="" />
    <img width={400} height={400} src={artBack} alt="" />
  </div>
</section>
